- name: Run scraper
  env:
    BASE44_API_KEY: ${{ secrets.BASE44_API_KEY }}
  run: |
    echo "Checking key..."
    if [ -z "$BASE44_API_KEY" ]; then echo "NO KEY FOUND"; exit 1; fi
    python scraper.py
