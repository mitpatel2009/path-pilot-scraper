- name: Run scraper
  env:
    BASE44_API_KEY: ${{ secrets.BASE44_API_KEY }}
  run: |
    echo "Checking BASE44 API key..."

    if [ -z "$BASE44_API_KEY" ]; then
      echo "❌ NO KEY FOUND - Secret is missing or not injected"
      exit 1
    fi

    echo "✅ Key found, running scraper..."
    python scraper.py
